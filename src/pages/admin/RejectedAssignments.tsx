import RejectedAssignments from '@/components/shared/RejectedAssignments';

export default function AdminRejectedAssignments() {
  return (
    <RejectedAssignments
      title="Rejected Assignments"
      description="All rejected assignments across all sections."
    />
  );
}

