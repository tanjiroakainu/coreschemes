import RejectedAssignments from '@/components/shared/RejectedAssignments';

export default function RegularStaffRejectedAssignments() {
  return (
    <RejectedAssignments
      title="Regular Staff Rejected Assignments"
      description="Rejected assignments for regular staff members."
    />
  );
}

